#!/bin/bash

set -o errexit
set -o nounset

NAME=$0
CMD=$1

case "$CMD" in
	enable)  VAL=true  ;;
	disable) VAL=false ;;
	*) err "usage: $NAME enable|disable"; exit 1;;
esac

gsettings set org.gnome.settings-daemon.peripherals.touchpad touchpad-enabled "$VAL"
