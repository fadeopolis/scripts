#!/bin/bash

set -o errexit
set -o nounset

NAME=$0
CMD=$1

case "$CMD" in
	enable)
		VAL=true
	;;
	disable)
		VAL=false
	;;
	*)
		err "usage: $NAME disable"
		exit 1
	;;
esac

## disable bluetooth
rfkill block bluetooth

