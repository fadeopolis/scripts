#!/usr/bin/fish

set DIR /home/fader/Desktop/git.LLVMtools

if [ ! -d $DIR ]
	echo "Could not find LLVMtools git repo"
end

pushd $DIR
color -f red (pwd)

git fetch -v --all

pushd work-tree
color -f red (pwd)

git pull

popd

pushd remote
color -f red (pwd)

git pull

popd
